# CLAUDE.md

Guidance for working in this repository. Keep this file up to date as the project grows.

## Project goal

Automated **reseller management & invoicing** for a VPN business running on **Hiddify** panels (~10 panels, ~400 resellers). Each month the system pulls usage from every panel, computes each reseller's invoice, sends it via a Telegram bot, collects **USDT (BEP-20)**, and (optionally) runs reminders + suspension for unpaid invoices. Replaces a manual desktop tool.

- **Phase 1:** localhost MVP — admin web panel + Telegram bot + Postgres + invoicing/dunning flow. Everything dockerized. **Complete.**
- **Phase 2 (in progress):** server deploy scaffolding lives in `deploy/` — one-line installer (`deploy/install.sh`, Ubuntu 24.04/26.04), `deploy/docker-compose.prod.yml`, and a **Caddy** reverse proxy (`deploy/Caddyfile`) for one-domain + automatic HTTPS. See `deploy/README.md`.

Full background and the resolved business decisions: see `docs/ARCHITECTURE.md`. (The earlier `MY_UNDERSTANDING.md` and the three reference folders were removed once their content was absorbed.)

## Architecture (one-liner)

`Hiddify panels → sync (backup JSON) → Postgres snapshot → invoice engine → Telegram bot + web panel → USDT payment verify → dunning/enforcement (Hiddify Admin API)`.

```
backend/   FastAPI API + APScheduler (sync, invoicing, dunning)
  app/core        config, db (async SQLAlchemy), crypto (Fernet), security (JWT/bcrypt)
  app/models      ORM: panels, resellers, end_user_snapshots, invoices, invoice_lines,
                  payments, settings, delivery_log, enforcement_actions, sync_runs, app_users
  app/schemas     Pydantic request/response models
  app/api         routers: auth, (panels, resellers, invoices, payments, debts, sales,
                  settings, dashboard — added per milestone)
  app/services    panel_client (backup-JSON + admin-API adapters), sync, invoice_engine,
                  pricing, payments, dunning, pdf, settings_service, bootstrap
  app/bot         aiogram v3 Telegram bot (membership gate, link match, delivery, payments)
  app/scheduler   APScheduler jobs
frontend/  React + Vite + TypeScript SPA (RTL, Persian) — MUI + Recharts
docs/      ARCHITECTURE.md + Mermaid diagrams
```

## Stack

- **Backend:** Python 3.12, FastAPI, SQLAlchemy 2.0 (async), Pydantic v2, APScheduler.
- **DB:** PostgreSQL 16 (prod/docker). `sqlite+aiosqlite` is supported for quick local runs/tests via `DATABASE_URL`.
- **Bot:** aiogram v3.
- **PDF:** reportlab + arabic-reshaper + python-bidi (Persian/RTL). **Dates:** Gregorian for billing periods; Jalali (`jdatetime`) for display.
- **Crypto/payments:** BEP-20 USDT; MVP verifies a submitted TXID via the BscScan API. HD-wallet per-reseller addresses are a deferred drop-in module.
- **Frontend:** React + Vite + TS + MUI (RTL theme, Vazirmatn) + Recharts.

## Data model essentials

- **panels** — one per Hiddify panel. `host`, `proxy_path` (encrypted), `owner_uuid`, `admin_api_key` (encrypted, for write/enforcement). Derives `backup_url` and `admin_api_base`.
- **resellers** — a panel admin (`mode` agent/admin). Keyed by `(panel_id, admin_uuid)`. Has `parent_admin_uuid` (hierarchy), `bot_chat_id` (set when they register in the bot), `price_per_gb` (nullable → global default), `exclude_from_billing`, `enforcement_state`, and `max_users_snapshot`/`max_active_users_snapshot` for exact restore.
- **end_user_snapshots** — latest snapshot per `(panel_id, user_uuid)`: `added_by_uuid`, `usage_limit_gb`, `start_date`, `enable`, etc.
- **invoices** + **invoice_lines** — one invoice per `(reseller, period_start, period_end)`.
- **payments**, **settings** (runtime, panel-editable), **delivery_log**, **enforcement_actions**, **sync_runs**.

## The invoice formula (source of truth — confirmed with owner)

For each reseller **and its descendant sub-resellers** (bundle via `parent_admin_uuid`), sum **`usage_limit_GB` (quota SOLD, not consumed)** of every end-user where:
- `added_by_uuid` ∈ the reseller/descendants, AND
- `start_date` is within the billing **Gregorian month** (service created that month), AND
- `usage_limit_GB > free_under_gb` — configs at/under the free threshold (default **1 GB**, so both 0.5 GB and 1 GB) are free test configs; **anything above (1.5, 5, …) IS billed**. Comparison is exact (not rounded), so a real 1.3 GB package is never mistaken for a test config. (`free_under_gb` is editable in Settings/pricing.)

`amount_toman = Σ usage_limit_GB × price_per_GB` (default 1000 T/GB; per-reseller override). **No** prior-unpaid carry-over. Convert to USDT via the configurable `toman_per_usdt` rate. The **Owner** (super_admin) and `exclude_from_billing` resellers are never billed.

**Re-generation safety:** one invoice per `(reseller, period)` (unique constraint). Re-running «صدور فاکتورهای دوره» recomputes only DRAFTs; **paid** invoices are never recomputed (even with `force`), and other delivered (sent/overdue/enforced) are skipped unless `force`. So generating twice / regenerating a past month never duplicates or disturbs settled accounting. «حذف پیش‌نویس‌ها» (`POST /api/invoices/discard-drafts`) deletes only DRAFT invoices (a run you don't want to keep) — delivered ones are untouched. Drafts are NOT written to the financial ledger; an invoice enters the ledger only when sent/paid.

**Durable financial ledger:** every invoice's money facts (panel, reseller, month, GB, amount, paid/unpaid, txid) are mirrored into `financial_records` (denormalized, no FK). This ledger is **never** deleted by the "wipe data" reset and survives panel/reseller removal — viewable under «تاریخچهٔ مالی». Written by `app/services/financial_archive.py` on generate/pay/edit/cancel/defer.

## Dunning & enforcement (real Admin-API actions; auto-suspend OFF by default)

Per unpaid invoice (timings + texts editable in settings): **D+2** reminder, **D+4** reminder, **D+5** hard warning + **enforcement** — via the Hiddify **Admin REST API**: disable the reseller's + sub-resellers' end-users and set the reseller's `max_active_users`/`max_users` to 0 (snapshot prior values first). The day-count anchors on `sent_at`, UNLESS a **payment deadline** (`deferred_until`) is set on the invoice — then the whole cycle **restarts from the deadline date** (paused until then; the defer endpoint clears prior reminder marks and restores an already-suspended reseller for the new window). `enforcement_enabled` defaults **False** (dry-run logs intended actions); the live API path is fully implemented — flip it on in Settings. On **confirmed payment**, auto-restore the snapshot.

## Security conventions

- **Never commit secrets.** Bot token, wallet seed/xpub, panel proxy paths/UUIDs/API keys, card numbers → `.env` (gitignored) or DB `settings` (encrypted). The three reference folders + `MY_UNDERSTANDING.md` are gitignored.
- Secret DB columns/settings are encrypted with Fernet (`app/core/crypto.py`) keyed off `SECRET_KEY`. The settings API masks secrets on read.
- Bootstrap config in `.env`; runtime config in DB, editable from the panel.

## Coding conventions

- `from __future__ import annotations`; modern type hints; SQLAlchemy 2.0 `Mapped[...]` / `mapped_column`.
- Async throughout (FastAPI, SQLAlchemy, aiogram, APScheduler). Each scheduler job opens its own session and never crashes the loop.
- Pure invoice math lives in `app/services/invoice_engine.py` and is unit-testable without a DB.
- Panel access goes through the `PanelClient` interface so read (backup JSON) and write (Admin API) adapters are swappable.
- Money: Toman as integer/Numeric; USDT as Numeric(…,6).

## Install & run (single production path — no separate local/dev version)

One command installs AND updates; re-running rebuilds to the latest release but
**preserves the database** (data is wiped only from the panel's «پاک‌سازی داده‌ها»):

```bash
curl -fsSL https://raw.githubusercontent.com/Alighaemi9731/hiddify-invoice-system/main/get.sh | sudo bash
```

It prints `http://<server-ip>`; the first visit shows the one-time **setup wizard**
(username / password / domain → auto-HTTPS). Stack = Caddy + SPA + FastAPI(+scheduler)
+ bot + Postgres, via `deploy/docker-compose.prod.yml`. The DB is Postgres only; the
backend image bundles `postgresql-client` so auto-backups capture a real `pg_dump` and
restores import via `psql`. Schema evolves on boot via `init_models()` +
`_sync_missing_columns` (dialect-aware `ADD COLUMN`). Running `backend/tests` (pytest,
`requirements-dev.txt`) is the only thing that touches SQLite — there is no local-run app variant.

## Milestone status

- [x] **M1** scaffold, models, auth, settings, docker, docs
- [x] **M2** panel sync + Panels CRUD + sample seed (`python -m app.cli.seed_sample`)
- [x] **M3** invoice engine + Toman→USDT + Persian PDF + invoices/resellers/reports(sales,debts,dashboard)/settings APIs
- [x] **M4** Telegram bot (aiogram): membership gate, link match, invoice delivery, TXID submit, delivery log
- [x] **M5** BEP-20 payment verification (BscScan TXID) + auto mark-paid + auto-restore hook
- [x] **M6** dunning + enforcement (Admin-API write adapter, reminders D+2/D+4/D+5, suspend/restore, dry-run default)
- [x] **M7** React RTL/Persian SPA (login, dashboard charts, panels, resellers, invoices, payments, debts, sales, logs, settings)
- [x] **M8** polish: unit tests (`backend/tests`), run docs
- [x] **M9** Phase 2 deploy: one-line installer/updater (latest release, DB preserved), Caddy auto-HTTPS, first-run setup wizard, security (captcha + rate-limit + TOTP 2FA), durable financial ledger. Single production-only code path (dev compose/Makefile/seeds removed).
- [x] **M34** Polish pass — RTL form bug, modern theme, complete Help, bot RTL/menus. (1) **RTL field bug** — many forms put `dir="ltr"` on the whole MUI `TextField` (for token/UUID/host/card/captcha inputs), which also flipped the Persian **label + helperText** to LTR and garbled them (e.g. «از @BotFather بگیرید.»). Fixed across Settings/Login/Panels/Setup/AccountBackup by scoping direction to the input only via `inputProps={{ dir: "ltr" }}` — label/helper stay RTL, input content stays LTR. (2) **Modern theme** (`theme.ts`) — refined palette + explicit `text` colors, glassy `AppBar` (backdrop blur), custom thin scrollbars, focus-ring + hover on inputs, contained-button shadow, table-row hover tint, rounded dialogs/tooltips/tabs indicator, heading letter-spacing. Uplifts every page at once (no per-page rewrites). (3) **Help page** fully rewritten + corrected — it still documented Dashboard buttons removed in M28 and missed Financial History, interim invoices, sub-reseller GB caps, capacity meter/bump, can_add_admin, card/screenshot payments, configurable schedules, self-update, main-resellers-only list. New «شروع و مفاهیم» group (quick-start + the invoice formula/free-config/dunning/ledger concepts) and accurate coverage of every panel page + both bot roles; search now spans ALL groups. (4) **Bot** — `/help` is role-aware + complete (owner vs reseller); owner menu greeting expanded; RTL fixes (leading ‏RLM / Persian-word prefix) on the debtors list, zero-sale list, «پنل‌های من»/sub-panel labels, and the remove-links buttons so lines starting with English reseller/panel names no longer left-align.
- [x] **M33** «نماینده‌های اصلی» everywhere — Resellers list + bot stats count only TOP-LEVEL resellers. The Resellers **«فهرست»** tab and the bot **«آمار کلی»** were counting/showing ALL resellers (every sub-reseller too), which double-represents people already visible in the tree view and inflates "how many resellers do I have". New shared `app/services/reseller_stats.py` is the single source of truth: `top_level_roots(resellers)` returns the non-owner roots (parent is the panel Owner, or structural-root fallback) keyed by `(panel_id, admin_uuid)` — EXACTLY the tree-view root logic, so the panel and the bot always agree; `load_root_stats(session, panel_id=None)` → `RootStats(total, billable, exempt, connected)` (billable = not `exclude_from_billing`; connected = billable roots with a `bot_chat_id`). (1) **List tab** now requests `GET /api/resellers?top_level_only=true` (new param: computes the root set from all of the panel's resellers, then keeps only those rows) — sub-resellers no longer appear (they're in «ساختار درختی»). A header line shows the count like the tree tab: «N نمایندهٔ اصلی (M معاف از فاکتور)». Tab relabelled «فهرست (نماینده‌های اصلی)». (2) **Bot «آمار کلی»** now reports «نمایندگان اصلی: {billable} ({connected} متصل به ربات)» (+ «… معاف» when any) instead of all-resellers/all-registered. Tests: `tests/test_reseller_stats.py` (owner/sub exclusion, billable/exempt/connected split, same-uuid-on-two-panels independence).
- [x] **M32** Settings tab redesign (curated, professional). The page was a generic auto-dump of every DB setting in storage order — messy, with internal-only toggles shown and rarely-used fields front-and-center. Rewrote `frontend/src/pages/Settings.tsx` as a **hand-authored, metadata-driven** UI: a **vertical sidebar navigation** (categories on the side + content panel; collapses to scrollable top tabs on mobile via `useMediaQuery`) with per-category icons. Each field carries its own `label`/`help`/`type`/`dir`/bounds and **conditional visibility** (`when`): card fields show only when card payment is on, `acme_email` only when HTTPS is on, USDT fields only when USDT is on. **Internal settings are hidden** (`setup_done`, `owner_chat_id`) — `owner_chat_id` instead drives a read-only «تلگرام مالک متصل است / هنوز /start نزده‌اید» status chip in the General section. **Rarely-used fields collapse into a per-section «تنظیمات پیشرفته»** (the static private channel/group link fields — useless for private chats since the bot auto-generates one-time invite links — plus contract/xpub/BscScan/tolerance/grace). Fields are grouped into titled sub-sections (e.g. Telegram → کانال / گروه / پاک‌سازی; Payments → روش‌های فعال / USDT / کارت / تأیید تراکنش) with dividers + help text, number inputs carry min/max mirroring the backend clamp, and a sticky header shows the unsaved-change count. A safety-net «متفرقه» section auto-catches any non-hidden setting not yet given a curated field, so a newly-added backend key is never silently dropped. Frontend-only; the settings API/storage is unchanged.
- [x] **M31** Owner-configurable scheduler timings (panel Settings → «زمان‌بندی کارهای خودکار»). M30 fixed the cron times but hardcoded them; now **all five jobs read their cadence from settings** and stay fixed-wall-clock cron. New `jobs.ScheduleConfig` + `load_config(session)` reads `invoice_day_of_month`/`invoice_hour` (monthly), `dunning_hour` (daily), `sync_interval_hours`, `guard_interval_minutes`, `backup_interval_hours` and clamps each to a safe range; `register(sched, cfg)` builds cron triggers from it. **Crucial clamp:** the "every N hours" steps go into a cron field (hour 0–23), so they're capped at **23** — `hour='*/24'` is an INVALID cron field and would throw at `add_job` (found by the pre-release adversarial review). `register` now constructs all five `CronTrigger`s **before** mutating the jobstore, so it's all-or-nothing (a bad value can never leave the running scheduler half-updated). **Live apply, no restart:** `scheduler.apply_settings(session)` re-reads config and re-registers (replace_existing rewrites each trigger in place); the settings API (`PUT`/`PATCH`) calls it after any schedule-key change. `scheduler.start()` → `_register_jobs` is now async (opens its own session to load config; falls back to defaults if that errors, so boot never crashes). Settings moved into the **`schedule`** group (incl. `backup_enabled`/`backup_interval_hours`, which were in a `backup` group NOT listed in the frontend's `GROUP_ORDER` → previously **invisible** in the panel). Frontend: generic number fields auto-render with per-key `min/max` bounds (mirroring the backend clamp) + a group note that all times are Tehran-local, fire on round boundaries regardless of deploy time, and apply immediately. Supersedes M30's closing note ("`backup_interval_hours` stays decorative") — it's now functional. Tests: `tests/test_scheduler_config.py` (clamping, trigger construction, the `*/24`-is-invalid boundary, defaults).
- [x] **M30** Schedulers on fixed wall-clock cron (deploy-independent) + Tehran timezone. **Root cause of "no auto-backup ever arrived":** the auto-backup (and panel-sync + channel-guard) were APScheduler `interval` jobs whose countdown anchors to process start, and the in-memory job store resets it on every restart — so a plain `interval, hours=2` job's FIRST fire is always start+2h. On a box being actively redeployed (each `get.sh`/compose rebuild bounces the backend, <2h apart) the 2h window never completes, so the scheduled backup **literally never fired** (verified: 0 files in the persistent `app_data/data/backups`, send-path itself healthy — a manual `send_backup_to_owner` delivered a 567 KB zip fine). **Fix:** all five jobs now use **cron** triggers at fixed round clock times, independent of deploy time — backup every 2h on the hour (`hour='*/2', minute=0` → 00,02,…,22), periodic_sync every 6h (`hour='*/6'` → 00,06,12,18), channel_guard every 10 min (`minute='*/10'`), daily_dunning 10:00, monthly_invoicing 1st@09:00. The scheduler timezone moved from UTC to **Asia/Tehran** (`SCHEDULER_TIMEZONE` env, default Asia/Tehran — Iran has no DST so it's a stable UTC+3:30) so round hours match the owner's clock; monthly/dunning now fire at those Tehran local times. Added `tzdata` to requirements as zoneinfo insurance (container already resolves Asia/Tehran natively; verified). NOTE: `backup_interval_hours` setting stays decorative (the interval is the fixed 2h cron) — the owner's ask was explicitly fixed round times, not a configurable interval.
- [x] **M29.1** Interim invoice polish — per-node PDFs + RTL fix + title spacing. (1) **Own + each sub get their OWN PDF.** Previously `_send_self_interim` sent the top reseller ONE bundled PDF (own + all subs). Now it sends **N+1 GB-only PDFs**: one for the reseller's OWN users (`invoice_pdf.render_own_usage_pdf` → new `reseller_report.node_invoice_own`, which sums ONLY `node.admin_uuid` users — NOT descendants) and one per direct sub-reseller (its whole subtree, via `render_node_usage_pdf`). So the main reseller's PDF lists only the users they created, each sub's PDF is separate, and own + each sub's subtree cover everyone exactly once (verified own 15GB + sub 20GB == bundle 35GB). The reseller can hand each sub its matching PDF without exposing the others; the grand Rial total stays in the TEXT message only. (2) **Telegram RTL fix** — every breakdown line now begins with a Persian word («حجم …», «نماینده …») so a line starting with an English (sub-)reseller name no longer left-aligns and reads garbled. (3) **PDF title** «فاکتور علی الحساب» uses a normal space (was ZWNJ «علی‌الحساب», which the reshaper glued together in the PDF). Telegram text keeps the ZWNJ form (renders fine there).
- [x] **M29** Volume-only invoice PDFs + reseller self-interim invoice. (1) **All invoice PDFs are now GB-only** — `build_invoice_pdf` strips every money element (price/Toman/USDT/rate/min-sale + the wallet & card payment boxes); it renders header + the user line-items (row, name, uuid slice, create-date, GB) + a total-GB summary block («مجموع حجم» + service count). The money args are kept in the signature for backward-compat but ignored. Pricing/payment stays in the bot's TEXT message (`tpl_invoice` + payment instructions), so a reseller can hand the PDF to a sub-reseller without exposing the owner's configured prices. New `invoice_title` param (default «فاکتور»). (2) **«📄 فاکتور علی‌الحساب من»** in the reseller bot menu (`menu:interim` → `_send_self_interim`): for the CURRENT month so far, identical in SCOPE to the real end-of-month invoice (own users + ALL sub-resellers bundled). Sends the GB-only PDF (`invoice_pdf.render_node_usage_pdf`) plus a text breakdown computed by `reseller_report.interim_breakdown`: own usage (GB+amount), each direct sub-reseller's subtree (GB+amount), and a grand Rial total — ALL priced at the MAIN reseller's single `price_per_gb` (so it exactly matches the real bundle; verified total_gb == `node_invoice`). Footer note states it's interim and the real payable invoice arrives at month start. Only TOP-LEVEL resellers get it (a sub is billed via its parent).
- [x] **M28** Dashboard toolbar cleanup. Moved «همگام‌سازی پنل‌ها» from the Dashboard to the Panels tab (new «همگام‌سازی همه» button → `syncAllPanels`, with the same poll-to-refresh as the per-row sync). Removed the redundant «اجرای یادآوری‌ها» (it just ran the same `run_dunning` the daily scheduler already runs) and «صدور و ارسال ماهانه» (duplicate of the Invoices tab's «صدور فاکتورهای دوره») from the Dashboard. Dashboard now shows only the period picker + charts; dropped its now-unused mutations/imports (runMonthly/runDunning/syncAllPanels/useMutation/useToast). The ops endpoints still exist for the scheduler/bot.
- [x] **M27.2** Panel «آخرین همگام‌سازی» now shows GREGORIAN date + time in Iran time. New `format.fmtDateTime` renders `YYYY-MM-DD HH:mm` via `toLocaleDateString("en-CA"/"en-GB", {timeZone:"Asia/Tehran"})`; the Panels column uses it (dir=ltr). Backend already stores `last_synced_at` as tz-aware UTC and serializes with a `Z`, so the browser converts to Tehran (UTC+3:30) correctly. (Other tables still use Jalali `fmtDate`.)
- [x] **M27.1** Hide the `(#link_tag)` suffix next to reseller names in the Resellers tab (list + tree). It's the Hiddify link fragment captured at registration; kept in the DB but no longer shown.
- [x] **M27** Guard grace period + non-member menu leak fix. (1) **Kick grace period** — `channel_guard.enforce_channel` now skips a non-registered bot user whose `BotUser.created_at` is within `kick_grace_minutes` (default **15**, editable in Settings → تلگرام). The guard still runs every 10 min, but a newcomer who joins right at a check tick is spared that round and removed on the NEXT cycle (the owner's exact ask), giving them time to register their panel link. New `counts["grace"]`. (2) **Menu-leak fix** — a stray text/photo from a NON-member used to show the reseller menu (only blocked on button tap). `_send_menu(..., bot=...)` is now gate-aware: a non-member gets the join prompt with a clickable `/start` instead of the menu; `on_text`/`cmd_menu` pass `bot`. Members are unaffected. Verified: fresh user spared / 30-min user kicked; non-member stray text → join prompt (no keyboard) / member → real menu. NOTE (audited, not changed): dunning anchors enforcement on `sent_at`, so a fresh scheduler start can evaluate already-overdue invoices on its first run — safe because `enforcement_enabled` defaults to dry-run and `EnforcementAction` dedups; flagged for a future "last-run" guard if needed.
- [x] **M26.1** Hide the `/` command menu in groups. `apply_command_menus` registered the reseller menu under `BotCommandScopeDefault`, so Telegram showed the `/start//pay/…` list in GROUPS where the bot is a member/admin (for the gate). Now it registers under `BotCommandScopeAllPrivateChats` and explicitly `delete_my_commands` for `BotCommandScopeAllGroupChats` + `BotCommandScopeDefault` (clears the legacy leak). Owner menu still scoped to the owner's private chat. Applies on bot startup (container restart).
- [x] **M26** Sub-reseller registration block + guard notify/cadence. (1) **Only TOP-LEVEL resellers may register in the bot.** `_handle_link` now calls `_is_top_level_reseller` (mirrors `invoice_engine.select_billable_roots`: a direct child of an Owner `is_owner` uuid; structural-root fallback when no Owner row), and rejects a sub-reseller with a message telling them their parent manages/bills them — so registration eligibility matches billing eligibility. (2) **Channel/group guard notifies the kicked user** in the bot (explains they were removed because they never registered their panel link, asks them to send it) and now runs **every 10 minutes** (was daily) — `counts["notified"]`, best-effort send (skips if they blocked the bot). (3) Confirmed: bot messaging persists across a DB restore on any server as long as the SAME `telegram_bot_token` (stored in settings, carried by backup) is used and the user hasn't blocked the bot. Verified: top-level A registers / sub B blocked; guard kicks+notifies a stranger while sparing a bound reseller.
- [x] **M25** Private-group registration + membership-gate hardening. (1) **Register a private GROUP** (you can't forward FROM a group — Telegram hides the group as the forward origin): when the owner registers a channel via `on_forward`, the bot now reads that channel's linked discussion group (`get_chat(channel).linked_chat_id`) and auto-registers it as `announcement_group_id`. Manual fallback: the owner can type «group -100…» / «channel -100…» (Persian «گروه/کانال» too) in the bot to set the id directly (`_SETCHAT_RE`, owner-gated). (2) **Gate-on-every-tap security fix** — the membership gate was only checked in `cmd_start`, so a user with the menu still in their chat history (or who left the channel/group afterward) could keep using old buttons. Added a `router.callback_query.outer_middleware` that re-checks `_missing_gates` on EVERY callback for non-owners and blocks with an alert; `check_membership`/`noop` are exempt so re-verification still works. Verified: middleware blocks a non-member's old `menu:*` tap, allows members, exempts `check_membership`; numeric-id regex + linked_chat path tested.
- [x] **M24** Bot replies in private chat only. When the bot is an admin of the announcement channel/group (required for the membership gate + guard), Telegram delivers every group message to it and it was replying with the menu to anything typed in the group. Added a single router-level guard `router.message.filter(F.chat.type == "private")`, so ALL message handlers ignore group/supergroup/channel messages and the bot only responds in private chats. Membership checks (get_chat_member API) and channel/group registration (`on_forward` reads `forward_origin`, forwarded in PV) are unaffected; callback queries (button taps) only occur on the bot's own PV messages.
- [x] **M23.3** Self-update auto-reload — real root cause + manual fallback. The poll loop used the default 120s axios timeout, so a request fired just before the backend bounced could hang the whole loop and never reach the `catch` that detects down→up. Now a dedicated `pingInfo(timeoutMs=4000)` probe drives completion: done when the live `/api/info` version differs from the version at click time (most reliable), OR on a down→up bounce. Status-file read is best-effort messaging only. Added an always-visible «بارگذاری مجدد صفحه» button during the updating state so the owner is never stuck. (Still takes full effect on the NEXT update since the running page serves pre-fix JS; the manual button covers the current one.)
- [x] **M23.2** Self-update auto-reload bugfix. After a successful update the panel stayed on «در حال بازسازی…» forever (manual refresh worked). The reload was gated on the watcher's `done` status file being read at the right instant — fragile. Now the frontend completes on EITHER signal: (a) `phase==done`, OR (b) the backend went unreachable and then recovered (the rebuild always bounces the container) — an `armed`+`sawDown` guard ensures a pre-rebuild network blip isn't mistaken for completion. Also added a 10-min watchdog message. (Takes effect on the NEXT update, since the running page serves the pre-fix JS; one manual reload loads it.)
- [x] **M23.1** Self-update path bugfix. The in-panel update watcher never worked on real installs: the compose bind-mount used `./update` which resolves from the compose file's dir (`deploy/`), so the backend saw `deploy/update` while the systemd watcher wrote to `<repo-root>/update` (`UPDATE_DIR=$REPO_DIR/update`) — two different dirs, so the panel always showed «سرویس به‌روزرسانی نصب نیست». Fixed the mount to `../update`. Also hardened detection: the watcher now writes a persistent `.updater-alive` marker on startup that survives the per-request status reset, and `update-status` reports `updater_installed` from it — so a poll during the brief window after clicking update no longer falsely reports "not installed". (Requires one more manual `get.sh` run to pick up the corrected compose; after that the button self-updates.)
- [x] **M23** Per-sub-reseller MONTHLY GB cap (the volume limit Hiddify lacks). A parent reseller can set, from the bot, a monthly SOLD-quota ceiling (GB) on each sub-reseller — Hiddify only caps user COUNT, so this fills a real gap. `Reseller.gb_cap` (int, NULL=no cap) + `gb_cap_alerted_period` (alert-once-per-month guard). In the bot's «مدیریت زیرمجموعه‌ها» → sub detail, `node_report` now also returns `gb_cap/current_gb/cap_pct/cap_remaining_gb` (current billable GB computed by the SAME invoice rule), shown as a colored text progress bar; a «تعیین/تغییر سقف حجم ماهانه» button (`subcap:<id>` → `SubCapState` FSM, accepts Persian digits, 0=remove) sets it. `gb_cap.check_caps()` runs after every periodic sync (re-evaluates against fresh data) and sends a ONE-TIME heads-up to the parent (and the sub) when the ceiling is reached — informational only, NO auto-suspend (the parent decides; suspend/restore buttons already exist). Cap resets each billing month; alert re-arms on month rollover or when the cap is changed. Verified end-to-end (report math excludes free configs, alert fires once then suppresses, re-arms on change).
- [x] **M22** In-panel self-update button. The owner can update to the latest release from «حساب و پشتیبان → به‌روزرسانی سامانه» — no server terminal. The backend container is sandboxed (no Docker socket), so a host-side watcher does the rebuild: `POST /api/ops/update` writes a `.update-requested` flag into a host-bind-mounted dir (`deploy/update` → `/app/data/update`, `UPDATE_DIR`); `GET /api/ops/update-status` reports phase (idle/requested/running/done/failed) + `updater_installed`. `deploy/updater.sh` (systemd unit `hiddify-updater`, installed/refreshed by `deploy/install.sh`) polls the flag every 5s and runs `get.sh` (pull latest tag + `docker compose up --build`, DB preserved), writing a status JSON the panel polls. The frontend shows the current version, a confirm-gated button, a progress bar, tolerates the brief downtime during rebuild, and auto-reloads on `done`. Older installs (no watcher yet) get a clear "run the installer once to enable" notice via `updater_installed:false`. Verified: endpoint state machine (idle→requested→running→done), watcher status JSON validity, all deploy scripts `bash -n` clean, compose YAML valid.
- [x] **M21** Auto-sync on panel add/edit (bugfix). A newly-added panel previously sat at status `unknown` (UI «در حال همگام‌سازی…») forever because `create_panel` never triggered a sync — only the manual button or the 6-hourly job did. Now `create_panel` schedules `_sync_one_bg(panel.id)` via `BackgroundTasks` (if enabled), and `update_panel` re-syncs when a connection-relevant field changes (host/proxy_path/admin_api_key/owner_uuid) — setting status back to `unknown` first. Frontend Panels page now polls every 3s **only while** a panel is `unknown`, so the chip flips to موفق/خطا on its own (no manual refresh). Verified live: create → status `unknown` + 1 bg task → after run, status `ok` with 61 resellers / 4169 users synced.
- [x] **M20** Forced-membership for a GROUP (alongside the channel) + panel toggles. The membership gate is now multi-target and data-driven: `_required_gates(session)` returns the enabled gates (channel and/or group), each gated by its own toggle (`channel_membership_required` default on, `group_membership_required` default off) AND a configured chat id; `_missing_gates`/`_gate_or_menu` require membership in EVERY enabled gate (AND). `cmd_start` + `cb_check_membership` use them; `membership_keyboard(targets)` now renders one join button per missing chat (private-friendly: per-user one-time invite link via `create_chat_invite_link`, else the static link). `on_forward` registers a forwarded **channel** → `announcement_channel_id`, a **group/supergroup** → `announcement_group_id`. The daily guard (`channel_guard.enforce_channel`) now sweeps BOTH chats under the SAME `channel_kick_enabled` switch — kicking bot users who aren't registered resellers (admins/creators protected, dry-run default). New settings: `announcement_group_id/_link`, `channel_membership_required`, `group_membership_required` (Settings → تلگرام, with a how-to note). All join links honor `one_time_invite_links`.
- [x] **M19** Card-to-card payment method + per-method on/off toggles. New `app/services/payment_methods.py` is the single source of truth: `load_options()` reads the toggles (`pay_usdt_enabled`/`pay_screenshot_enabled`/`pay_card_enabled`, default on/on/off) + their data (`usdt_bep20_address`, `card_number`, `card_holder_name`), and a method is only offered if its data is actually present (enabled-but-unconfigured is silently skipped). `instructions_text()` builds the multi-method instructions shown in (a) the bot «پرداخت» view (`_send_pay`), (b) the invoice Telegram caption (`delivery.build_invoice_text` — `tpl_invoice` now uses a `{payment_instructions}` placeholder instead of the hard-coded `{wallet_address}`/BEP-20 line; `seed_defaults` one-time-migrates any legacy template still on `{wallet_address}`), and (c) the PDF (`pdf.build_invoice_pdf` gained `card_number`/`card_holder` → renders a second payment box; `invoice_pdf.render_invoice_pdf` feeds it from the options). Toggles + card fields live in Settings → «روش‌های پرداخت». The sub-reseller PDF stays wallet/card-blank (off-system settlement). Verified: legacy-template migration, option gating, instruction text, and a real 2-box PDF render.
- [x] **M18** Admin capacity & sub-admin control (Resellers tab). Both are real Hiddify Admin-API capabilities now surfaced in the panel. (1) **Capacity meter** — a new «پُری ظرفیت» column shows a colored bar of how full each admin's user quota is: users this admin CREATED (counted from `end_user_snapshots.added_by_uuid`, this admin only — the panel stores no live count) over its `max_users`; blue <70%, amber ≥70%, red ≥90% (`CapacityBar`). One grouped query (`_usage_counts`) feeds both list + tree. (2) **«افزایش ظرفیت» button** — a dialog with quick +50/100/200/500 chips + a free-entry field; `POST /api/resellers/{id}/bump-limits` reads the admin's CURRENT limits from the API (so repeat clicks compound), adds the amount to BOTH `max_users` and `max_active_users`, writes via the admin API, persists locally. (3) **«ساخت زیرمجموعه» toggle** — a per-row Switch bound to Hiddify's `can_add_admin` (now parsed in `base.PanelAdmin`, stored on `Reseller.can_add_admin`, synced each run); `POST /api/resellers/{id}/can-add-admin` flips it live. New `app/services/admin_capacity.py` + `AdminApiClient.get_admin`/`set_can_add_admin`/`_patch_admin` (shares the v12 500-bug verify-via-GET path with `set_admin_limits`). Verified live on the test panel (read limits/usage, bump +100, toggle, then restored to original).
- [x] **M17** Bot UX & owner activity feed. (1) **Rejection notice** — `reject_payment` now tells the customer their payment wasn't accepted (`tpl_payment_rejected`), but only on a real state CHANGE to rejected (re-reject is silent); confirm likewise notifies only on change to confirmed — so toggling/double-clicks don't spam. (2) **Mistyped input → menu** — `on_text` fallback (and any unknown `/command`) now shows the correct main menu via `_send_menu` (owner menu for the owner, reseller menu otherwise) instead of a dead-end hint. (3) **Richer owner bot menu** — added «🟡 فروش صفر این ماه» (`_owner_zerosale`), «🔄 همگام‌سازی پنل‌ها» (`owner:sync`), «🔔 اجرای یادآوری‌ها» (`owner:dunning`), «🗄 پشتیبان‌گیری اکنون» (`owner:backup`); heavy monthly-issue stays in the web panel. (4) **Broadcast to one panel** — `broadcast_audience_keyboard` gained «🖥 نمایندگان یک پنل» → panel picker (`bcaud:panel:<id>`, `broadcast_panel_keyboard`); the service already supported `audience="panel"`. (5) **Owner activity feed** — owner now gets a Telegram ping on the important events (like the panel's logs tab): a new TXID payment awaiting review or auto-confirmed on-chain (`_handle_txid`), a screenshot payment (photo forwarded, already), and a new reseller registering in the bot (`_handle_link`). Dunning/monthly/abuse owner pings already existed.
- [x] **M16** Multi-invoice settlement on manual confirm. The auto-verify path already settled the customer's due-now invoices oldest-first by on-chain amount (`_settle_due_now`); the MANUAL/screenshot confirm path previously paid only the single linked (oldest) invoice. Now `confirm_manually(session, payment_id, invoice_ids=...)` settles the EXACT set the owner picks. The panel confirm button opens a dialog (`GET /api/payments/{id}/due-invoices` → the customer's due-now invoices across all their reseller rows, oldest first) with checkboxes + running total + «تأیید همهٔ بدهی» / «تأیید فاکتورهای انتخاب‌شده» (and the proof image inline). Every confirm records `Payment.settled_invoice_ids` (comma list); `reject_payment` reverts EXACTLY those invoices (not just the primary), so a multi-invoice confirm is fully reversible. So one transfer can clear several invoices and the owner controls which — a customer paying for 2 invoices no longer leaves one stranded.
- [x] **M15** Payment UX & safety. (1) **Screenshot payments** — a reseller can pay by sending a deposit **photo** to the bot (`@router.message(F.photo)` → `_handle_payment_proof`), not just a TXID; the image is saved to `data/payment_proofs/payment_<id>.jpg`, a `Payment(method=screenshot, status=pending)` is linked to their oldest due invoice, and the photo is **forwarded to the owner's Telegram** + shown in the web panel (`GET /api/payments/{id}/proof`, `has_proof` flag, 🖼 «مشاهدهٔ رسید»). A reseller who sends the screenshot as a *file* is nudged to resend as a photo. (2) **Dunning hold while pending** — `run_dunning` skips ALL reminder/warning/enforcement steps for any invoice with a `pending` payment, and never auto-suspends a reseller who has any pending payment (`held_invoice_ids`/`held_reseller_ids`, `on_hold` count). So a customer awaiting the owner's confirmation is never auto-suspended; the hold lifts on confirm (→paid) or reject (→cycle resumes). (3) **Reversible decisions** — `payments.reject_payment` reverts a previously-confirmed invoice back to owed (un-pays it, updates the ledger), and `confirm_manually` works on a rejected payment (re-pays + restores) and only notifies the reseller on the FIRST confirm. Web-panel confirm/reject buttons stay enabled for every status (behind a confirm dialog) so a mis-click is always recoverable. Manual confirm does NOT auto-re-suspend on reject (dunning re-escalates on its timeline).
- [x] **M14** Per-sub-reseller invoices. A reseller bundled with its sub-resellers still gets ONE PDF for the whole subtree (the owner bills the top node — unchanged). NEW: in the bot's «مدیریت زیرمجموعه‌ها» view, each sub-reseller now shows per-month «📄 فاکتور <ماه>» buttons that build an on-demand PDF rooted at that sub (`reseller_report.node_invoice` → `invoice_pdf.render_sub_invoice_pdf`, `subinv:<sub_id>:<label>` handler) so a reseller can bill each of their sub-resellers separately. The sub PDF is base-only (sold-quota × the sub-node's `price_per_gb`), NOT persisted as an Invoice (the owner's invoice already covers the subtree), wallet address blank (reseller↔sub settlement is off-system), issuer = the requesting reseller's name. Returns "no sales" when the sub has zero billable usage that month.
- [x] **M13** Abuse-resistant metering (billing model "C", `app/services/metering.py`, `metering_enabled` default on). Runs inside the existing 6h sync (no extra cost): per-user reset-aware cumulative usage + provisioned-quota buffer on `EndUserSnapshot.meter_*`, and a monthly `usage_meters` row. Billing ADDS the abuse the snapshot rule misses, on top of the unchanged base: **overage** (consumption past the paid buffer → catches "reset usage daily so a small package never ends") + **edit_renewal** (quota topped up without a new `start_date` → catches renew-by-edit). On the first delivery of an affected invoice the reseller gets a per-user breakdown (which user, what, that it's billed) and the owner gets a heads-up (`notify_abuse_if_any`). Normal billing is untouched and stays correct even before metering has synced.
- [x] **M12** Invoice correction & payment robustness. (1) Per-invoice «بازمحاسبه از روی پنل» (`POST /api/invoices/{id}/recompute`, `invoicing.recompute_invoice`) — syncs the panel then refreshes that one invoice's figures from current data, keeping its status (paid invoices protected); the tool to fix an already-sent invoice after the reseller corrected the panel. (2) Payment settles the customer's DUE-NOW invoices oldest-first across all their reseller rows in ONE transfer (`payments._settle_due_now`); deferred-to-future invoices are excluded from both the bot «pay» total and settlement. (3) Resending an invoice deletes the previously delivered Telegram message (`delivery_log.tg_message_id`) so the reseller's chat shows only the latest version.
- [x] **M11** Backup now carries `secret_key` in meta.json (restore writes it to `.env` first) so a cross-server restore can decrypt the encrypted settings — restore is self-sufficient. (Note: a bulk "reset admin passwords" feature was considered but removed — Hiddify v12's REST API has no admin-password field; only the web form sets it.)
- [x] **M10** No-terminal ops: panel «راه‌اندازی مجدد سرویس» button + auto-restart after restore (`/api/ops/restart`, process exits → Docker `unless-stopped` restarts; pooled conns dropped via `engine.dispose()`/`pool_pre_ping`). Bot sub-reseller management: a reseller picks a panel → lists their sub-resellers → views a report (user count, per-month sold-quota sales via `reseller_report.node_report`) → **suspend/restore** that sub-reseller (reuses `enforcement.enforce_reseller`/`restore_reseller` with `dry_run=False`; ownership-gated by `_owns_sub`).

**Phase 1 MVP is complete and runnable.** Phase 2 (installer/domain/SSL/systemd) is intentionally NOT built.

Backend API surface: auth, panels, resellers, invoices, payments, reports (sales/sales-by-panel/debts/dashboard/delivery-log/enforcement-actions), operations (dunning/run, run-monthly), settings. Scheduler jobs: monthly invoicing, daily dunning, periodic sync.
